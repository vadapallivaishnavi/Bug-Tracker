# Copy this to .env (root of the repo) if you ever need to change the
# frp public IP/port without editing docker-compose.yml directly.
# docker-compose reads .env automatically from the project root.

REMOTE_API_URL=http://13.60.166.135:6038/api
